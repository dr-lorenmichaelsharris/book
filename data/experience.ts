export const experienceSettings = {
  primaryCta: { label: 'Join the Book Community', href: '#join' },
  newsletterEndpoint: '', // Configure only when the server-side subscription handler is ready.
};
