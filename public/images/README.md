# Production image assets

Add the approved, rights-cleared publishing assets here before launch:

- `book-cover.jpg`
- `sanjiv-chopra.jpg`
- `loren-michaels-harris.jpg`

The current interface intentionally displays designed placeholders rather than fabricating a cover or author portraits. Asset paths, video URLs, calls to action, and optional sections are centralized in `data/book.ts`.
